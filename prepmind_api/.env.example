SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# No JWT secret needed — JWTs are verified via JWKS (ECC P-256 public key)
# JWKS endpoint is fetched automatically from: {SUPABASE_URL}/auth/v1/.well-known/jwks.json
APP_ENV=development
ALLOWED_ORIGINS=["http://localhost:3000"]

