class AppConstants {
  AppConstants._();

  static const appName = 'PrepMind AI';
  static const freeAiRequestLimit = 20;
  static const freeDocumentLimit = 5;
  static const maxFileSizeBytes = 20 * 1024 * 1024; // 20 MB
  static const supportedFileTypes = ['pdf', 'docx', 'pptx'];
}
