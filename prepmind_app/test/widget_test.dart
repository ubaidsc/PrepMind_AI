import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full integration tests require Supabase + dotenv initialization.
    expect(true, isTrue);
  });
}
